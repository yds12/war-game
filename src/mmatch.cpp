#include "mmatch.h"

MMatch::MMatch(Drawer* drawer, Game* game, Keyboard* keyboard, Mouse* mouse, Options* opt) : Match(drawer, game, keyboard, mouse, opt)
{
	//warmap = new WarMap(drawer, keyboard, mouse, options);
}

void MMatch::update()
{
	//warmap->update();
}

void MMatch::draw()
{
	//warmap->draw();
}

